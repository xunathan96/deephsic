# -----------------------
#   EVALUATION SETTINGS
# -----------------------

seed: 0
model:
  tied: True
  k: !include config/model/deepkernel/mlp/mlpB.dim2.yml
  l: !include config/model/deepkernel/mlp/mlpB.dim2.yml

dataset:
  test: !dataset.HDGM
    name: HDGM-4
    size: 10000
    dim: 4

dataloader:
  test: !torch.DataLoader
    batch_size: 100
    shuffle: True
    drop_last: True
    num_workers: 0
    pin_memory: True

