# -----------------------
#   EVALUATION SETTINGS
# -----------------------

seed: 0
model: !include config/model/deepkernel/mlp/mlpB.dim20.yml

dataset:
  test: !dataset.HDGM
    size: 50000  # 500 samples * 100 batches
    dim: 20

dataloader:
  test: !torch.DataLoader
    batch_size: 500  # each permutation test on 500 samples
    shuffle: True
    drop_last: True
    num_workers: 0
    pin_memory: True

