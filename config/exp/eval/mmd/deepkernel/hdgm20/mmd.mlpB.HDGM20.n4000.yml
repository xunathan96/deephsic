# -----------------------
#   EVALUATION SETTINGS
# -----------------------

seed: 0
model: !include config/model/deepkernel/mlp/mlpB.dim20.yml

dataset:
  test: !dataset.HDGM
    size: 400000  # 4000 samples * 100 batches
    dim: 20

dataloader:
  test: !torch.DataLoader
    batch_size: 4000  # each permutation test on 4000 samples
    shuffle: True
    drop_last: True
    num_workers: 0
    pin_memory: True

