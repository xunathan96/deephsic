# -----------------------
#   EVALUATION SETTINGS
# -----------------------

seed: 0
model: !include config/model/deepkernel/mlp/mlpA.dim4.yml

dataset:
  test: !dataset.HDGM
    size: 200000
    dim: 4

dataloader:
  test: !torch.DataLoader
    batch_size: 2000
    shuffle: True
    drop_last: True
    num_workers: 0
    pin_memory: True

