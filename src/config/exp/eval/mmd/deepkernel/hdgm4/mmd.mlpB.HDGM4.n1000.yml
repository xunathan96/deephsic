# -----------------------
#   EVALUATION SETTINGS
# -----------------------

seed: 0
model: !include config/model/deepkernel/mlp/mlpB.dim4.yml

dataset:
  test: !dataset.HDGM
    size: 100000
    dim: 4

dataloader:
  test: !torch.DataLoader
    batch_size: 1000
    shuffle: True
    drop_last: True
    num_workers: 0
    pin_memory: True

