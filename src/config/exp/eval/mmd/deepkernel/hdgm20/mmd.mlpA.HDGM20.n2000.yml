# -----------------------
#   EVALUATION SETTINGS
# -----------------------

seed: 0
model: !include config/model/deepkernel/mlp/mlpA.dim20.yml

dataset:
  test: !dataset.HDGM
    size: 200000  # 2000 samples * 100 batches
    dim: 20

dataloader:
  test: !torch.DataLoader
    batch_size: 2000  # each permutation test on 2000 samples
    shuffle: True
    drop_last: True
    num_workers: 0
    pin_memory: True

