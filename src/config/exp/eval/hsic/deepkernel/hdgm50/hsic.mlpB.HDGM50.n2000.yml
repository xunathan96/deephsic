# -----------------------
#   EVALUATION SETTINGS
# -----------------------

seed: 0
model:
  tied: False
  k: !include config/model/deepkernel/mlp/mlpB.dim25.yml
  l: !include config/model/deepkernel/mlp/mlpB.dim25.yml

dataset:
  test: !dataset.HDGM
    name: HDGM-50
    size: 200000  # 2000 samples * 100 batches
    dim: 50

dataloader:
  test: !torch.DataLoader
    batch_size: 2000  # each permutation test on 2000 samples
    shuffle: True
    drop_last: True
    num_workers: 0
    pin_memory: True

