# -----------------------
#   EVALUATION SETTINGS
# -----------------------

seed: 0
dataset:
  test: !dataset.HDGM
    size: 200000
    dim: 20
dataloader:
  test: !torch.DataLoader
    batch_size: 2000
    shuffle: True
    drop_last: True
    num_workers: 0
    pin_memory: True
