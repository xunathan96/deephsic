# -----------------------
#     ResNet-Gaussian
# -----------------------

name: resnet18x32-bandwidth
tied: False
k: !kernel.DeepKernel
  featurizer: !model.ResNet
    inter_channels: [64, 128, 256, 512]
    n_blocks: [2, 2, 2, 2]
    in_channels: 3
    out_features: 32
    activation: ELU
    block_type: basic
  feature_kernel: !kernel.Gaussian
    scale: 1
    bandwidth: 1
    trainable: True
  smoothing_kernel: !kernel.Gaussian
    scale: 1
    bandwidth: 400
    trainable: True
  eps: 0.01
  trainable: True
l: !kernel.Gaussian
  scale: 1
  bandwidth: 1
  trainable: True
