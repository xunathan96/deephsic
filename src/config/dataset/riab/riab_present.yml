# -----------------------
#    DATASET SETTINGS
# -----------------------
name: RatInABox

train: !dataset.RatInABox
  root: data/riab/raw/riab-10000.pkl
  size: 4000
  split: train
  train_val_test_split: '2:1:1'   # 2:1:1
  window: present
  transform: !transform.Compose
    - !transform.NumpyToTensor

val: !dataset.RatInABox
  root: data/riab/raw/riab-10000.pkl
  size: 4000
  split: val
  train_val_test_split: '2:1:1'
  window: present
  transform: !transform.Compose
    - !transform.NumpyToTensor

test: !dataset.RatInABox
  root: data/riab/raw/riab-10000.pkl
  # size: 4000
  split: test
  train_val_test_split: '2:1:7'
  window: present
  transform: !transform.Compose
    - !transform.NumpyToTensor
