# -----------------------
#    DATASET SETTINGS
# -----------------------
name: HDGM-20-n16000

train: !dataset.HDGM &data
  size: 16000
  dim: 20
  split: train
  train_val_test_split: '7:1:2'

val: !dataset.HDGM
  <<: *data
  split: val

test: !dataset.HDGM
  # <<: *data
  # split: test
  size: 10000   # 3200
  dim: 20
