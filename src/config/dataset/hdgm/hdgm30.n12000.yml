# -----------------------
#    DATASET SETTINGS
# -----------------------
name: HDGM-30-n12000

train: !dataset.HDGM &data
  size: 12000
  dim: 30
  split: train
  train_val_test_split: '7:1:2'

val: !dataset.HDGM
  <<: *data
  split: val

test: !dataset.HDGM
  # <<: *data
  # split: test
  size: 10000   # 2400
  dim: 30
